# Environment Variables for Vercel Deployment
# Copy these to your Vercel project's environment variables

# Email Configuration
EMAIL_HOST_PASSWORD=your_gmail_app_password_here

# Debug settings (set to False for production)
DEBUG=False

# Secret key (use the generated key from deployment guide)
SECRET_KEY=woy*3e6#a#r1yqwr8c&0=rv7*q9#zqmo#^())su3glb*g%r&p(

# Allowed hosts (replace with your actual Vercel domain)
ALLOWED_HOSTS=your-vercel-domain.vercel.app,localhost,127.0.0.1
