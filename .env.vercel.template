# Environment Variables Template for Vercel Deployment
# Copy these variable names to your Vercel project's environment variables
# DO NOT put actual values here - this file will be committed to Git

# Email Configuration
EMAIL_HOST_PASSWORD=your_gmail_app_password_here

# Debug settings (set to False for production)
DEBUG=False

# Secret key (generate a new one using Django's get_random_secret_key())
SECRET_KEY=generate_a_new_secret_key_for_production

# Allowed hosts (replace with your actual Vercel domain)
ALLOWED_HOSTS=your-vercel-domain.vercel.app,localhost,127.0.0.1

# Instructions:
# 1. Generate SECRET_KEY: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# 2. Get EMAIL_HOST_PASSWORD from Gmail App Passwords
# 3. Replace your-vercel-domain with your actual Vercel subdomain
